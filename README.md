# data-qa Skill

TPO 数据分析 Skill,支持对 xlsx/csv 文件进行智能问答分析。

## 功能特点

### v4.0 新增能力

- **智能意图识别**: 自动理解用户问题类型(查询、排名、趋势、对比、异常检测)
- **多维度分析**: 支持6种查询类型,深度分析 CSV 数据
- **答案自动验证**: 检查答案的准确性和完整性
- **异常检测**: 可配置阈值检测数据异常(默认30%)
- **场景对比**: 对比不同月份的场景数据变化

### 基础能力

- **单月分析模式**: 默认返回最新月数据,支持指定月份查询
- **精确趋势对比**: 仅与相邻月对比、年度同比,不统计多月趋势
- **场景数据按实有**: 有啥说啥,不编造缺失场景
- **跨会话记忆**: 支持数据记忆和问答历史

## 业务领域知识

### 核心指标

| 指标 | 定义 | 计算公式 |
|------|------|---------|
| **TPO** | 人工进线量与完单量的比值 | `人工进线量 / 完单量 × 10000` |
| **YOY** | 年度同比(Year over Year) | `(本期值 - 上年同期值) / 上年同期值 × 100%` |
| **MoM** | 环比(Month over Month) | `(本期值 - 上月值) / 上月值 × 100%` |

### 场景名称映射

| 原始字段名 / 旧名称 | 当前业务名称 |
|---------------------|-------------|
| 微信渠道客诉 | **资产权益** |
| 二次号 | **售后服务** |

### 十大场景数据规则

数据文件中实际包含的场景会因月份而异:
- 2025/1-2025/10: 费用疑义、营销活动、二次号(3个场景)
- 2025/11-2026/3: 费用疑义、营销活动、售后服务、资产权益(4个场景)

**有啥说啥,不编造缺失场景**

## 文件结构

```
data-qa/
├── SKILL.md              # Skill 核心定义
├── .gitignore            # Git 忽略文件
├── README.md             # 本文件
├── ITERATION_SUMMARY.md  # 迭代历史
├── scripts/              # 核心脚本
│   ├── qa_engine_v3.py   # 分析引擎 v3
│   ├── data_parser.py    # 数据解析器
│   ├── memory_manager.py # 记忆管理
│   └── xlsx_reader.py    # Excel 读取器
├── eval/                 # 测试用例
│   ├── evals_v3.json     # v3 评估用例
│   └── run_evals.py      # 测试运行器
├── references/           # 参考文档
│   ├── data-structures.md
│   ├── output-format.md
│   └── query-patterns.md
└── memory/               # 运行时记忆(本地存储,不上传)
    ├── datasets.json     # 数据集记忆
    └── qa_memory.db      # 问答历史(SQLite)
```

## 使用方法

### 智能问答(推荐 v4.0)

```bash
python scripts/qa_engine_v4.py --file <csv_path> --question "<问题>"
```

**支持的问题类型**:
- 月份数据查询: "最新月的费用疑义场景数据"、"2026/3 的数据"
- 场景查询: "各场景的 TPO"、"费用疑义的占比"
- 趋势查询: "最新月的趋势"、"同比和环比情况"
- 排名查询: "排名前3的场景"、"TPO 最高的场景"
- 异常检测: "检测异常"、"查看是否有波动"
- 对比查询: "对比最近两个月"、"2026/2 vs 2026/3"

**示例**:
```bash
# 查询最新月数据
python scripts/qa_engine_v4.py --file D:\data\test.csv --question "最新月的数据"

# 查询特定场景
python scripts/qa_engine_v4.py --file D:\data\test.csv --question "费用疑义的场景数据"

# 排名查询
python scripts/qa_engine_v4.py --file D:\data\test.csv --question "排名前3的场景"

# 异常检测
python scripts/qa_engine_v4.py --file D:\data\test.csv --question "检测异常"

# 对比两个月
python scripts/qa_engine_v4.py --file D:\data\test.csv --question "对比最近两个月"
```

### 单月分析(简单模式 v3)

```bash
python scripts/qa_engine_v3.py --file <csv_path> [--month <YYYY/M>]
```

**示例**:
```bash
# 分析最新月份
python scripts/qa_engine_v3.py --file D:\data\test.csv

# 分析指定月份
python scripts/qa_engine_v3.py --file D:\data\test.csv --month 2025/10
```

### 解析文件

```bash
python scripts/data_parser.py --file <xlsx_or_csv_path> --memory-dir memory/
```

### 记忆管理

```bash
# 查看所有数据记忆
python scripts/memory_manager.py --list-datasets

# 查看问答历史
python scripts/memory_manager.py --list-qa --limit 20

# 清除指定文件的记忆
python scripts/memory_manager.py --clear-dataset <file_hash>

# 搜索历史问答
python scripts/memory_manager.py --search-qa "<关键词>"
```

### 运行测试

```bash
# v4.0 完整功能测试
python scripts/test_qa_v4.py

# v3 评估测试
python eval/run_evals.py
```

## 输出格式规范

**核心原则**:
1. **不累计**: 不计算多个月的累计变化
2. **精确描述**: 禁止使用模糊形容词
3. **默认最新月**: 用户未指定月份时,默认返回最新月份
4. **仅相邻对比**: 趋势仅与上个月和去年同期对比
5. **场景按实有**: 有啥说啥,不编造缺失场景

**输出结构**:
```
## 查询结果

**问题**: [用户原始问题]
**月份**: [数据所属月份]

**结论**: [一句话核心答案]

**详细数据**:
[表格或列表]

**趋势对比**:
- 环比(vs上月): [数据]
- 同比(vs上年同月): [数据]

**状态**: [OK]/[!]/[X]
```

## 注意事项

1. 所有数据存储在本地,不上传云端,保护数据隐私
2. 渠道名称统一使用当前业务名称(资产权益、售后服务)
3. 2025/11 和 2025/12 的 YOY 为空属于正常,无需提示异常
4. 场景数据按实有场景展示,禁止编造其他场景
5. 数值精度默认保留2位小数

## License

MIT

## Author

Generated by AI Assistant
